<%= form_for(@medical_exam) do |f| %>
  <% if @medical_exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_exam.errors.count, "error") %> prohibited this medical_exam from being saved:</h2>

      <ul>
      <% @medical_exam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :emp_id %><br>
    <%= f.text_field :emp_id %>
  </div>
  <div class="field">
    <%= f.label :exam_type %><br>
    <%= f.text_field :exam_type %>
  </div>
  <div class="field">
    <%= f.label :exam_time %><br>
    <%= f.datetime_select :exam_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
