<li>
  <%= gravatar_for user, size: 52 %>
  <h3> <%= user.name%> </h3>
  <% if !current_user?(user) %>
    <div class="pull-right"> <%= link_to "Delete", user, method: :delete,
                                  data: { confirm: "You sure?" }, class: "btn btn-primary" %> </dic>
  <% else %>
  	<div class="pull-right"> <%= link_to "Edit", edit_user_path(user), class: "btn btn-primary"  %> </div>
  <% end %>

</li>